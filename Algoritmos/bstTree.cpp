#include <iostream>

using namespace std;

struct Node {
    Node *left;
    int value;
    Node *right;
};

int main() {
    Node *source, *target;
    int N = 0, x = 0, L = 0, R = 0;
    bool verif;
    
    while (1) {
        cin >> N;

        for (int i = 0; i < N; i++) {
            cin >> x;
        }

        for (int i = 0; i < N; i++) {
            cin >> x; 
        }
    }
}